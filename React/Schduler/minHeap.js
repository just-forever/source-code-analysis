// Vnode
const vNode = [3,6,4,9,2,1,7];
const heap = []; // 
const stack = [];

for(let i = 0, len = vNode.length; i< len; i++) {
    const index = heap.length;
    const node = arr1[i];
    heap.push(node);
    siftUp(heap, node, index);
}

while(heap.length) {
    const f
}